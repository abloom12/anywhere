// const matchedLayouts = layoutDefs
//     .filter(ld =>
//       ld.rawSegments.length <= routeDef.rawSegments.length &&
//       routeDef.rawSegments
//         .slice(0, ld.rawSegments.length)
//         .every((seg,i) => seg === ld.rawSegments[i])
//     )
//     // 2) sort outermost→innermost
//     .sort((a,b) => a.rawSegments.length - b.rawSegments.length);
